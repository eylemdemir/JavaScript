import chalk from "chalk";

export const logRed = (message) => {
    console.log(chalk.red(message));
};

export const logGreen =(message) =>{
    console.log(chalk.green(message));
};

export const logBlue = (message) =>{
    console.log(chalk.blue(message));
};